spring:
  application:
    name: gateway_service
  profiles:
    active: dev
  config:
    import: 'configserver:'
  cloud:
    config:
      username: spring_cloud_user
      password: '1234'

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
