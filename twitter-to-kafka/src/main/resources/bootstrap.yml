spring:
  application:
    name: twitter_to_kafka_service
  profiles:
    active: dev
  config:
    import: 'configserver:'
  cloud:
    config:
      username: spring_cloud_user
      password: '1234'


